# OrderFlow Command Center

Standalone React frontend for the order processing command center.

## Run locally

```bash
npm install
npm run dev
```

The app currently uses static mock data and does not call the Spring backend.
